{
  "name": "authentication",
  "version": "0.0.1",
  "description": "Authentication service for Infragis Platform",
  "main": "dist/main/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/abelinsky/infragis"
  },
  "author": "Alexander Belinsky",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && node_modules/typescript/bin/tsc -p tsconfig-build.json && npm run lint",
    "start": "ts-node-dev --respawn --transpile-only src/main",
    "lint": "tsc --noEmit && eslint \"**/*.{js,ts}\" --quiet --fix",
    "migrate": "npm run build && node_modules/.bin/sequelize db:migrate --env development",
    "migrate:undo": "npm run build && node_modules/.bin/sequelize db:migrate:undo --env development",
    "migrate:undo:all": "npm run build && node_modules/.bin/sequelize db:migrate:undo:all --env development",
    "test": "jest --passWithNoTests --silent --noStackTrace --runInBand",
    "test:verbose": "jest --passWithNoTests --runInBand",
    "test:unit": "npm test -- --watch -c jest-unit-config.js",
    "test:integration": "npm test -- --watch -c jest-integration-config.js",
    "test:staged": "npm test -- --findRelatedTests",
    "test:ci": "npm test -- --coverage",
    "test:coveralls": "npm run test:ci && coveralls < coverage/lcov.info"
  },
  "keywords": [],
  "dependencies": {
    "@infragis/common": "0.0.87",
    "@types/body-parser": "^1.19.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "helmet": "^4.1.0",
    "inversify": "^5.0.1",
    "inversify-express-utils": "^6.3.2",
    "module-alias": "^2.2.2",
    "reflect-metadata": "^0.1.13",
    "utility-types": "^3.10.0"
  },
  "devDependencies": {
    "@types/eslint": "^7.2.0",
    "@types/express": "^4.17.7",
    "@types/helmet": "0.0.47",
    "@types/inversify": "^2.0.33",
    "@types/jest": "^26.0.10",
    "@types/module-alias": "^2.0.0",
    "@types/node": "^14.0.27",
    "@typescript-eslint/eslint-plugin": "^3.9.0",
    "@typescript-eslint/parser": "^3.9.0",
    "eslint": "^7.7.0",
    "jest": "^26.4.0",
    "ts-jest": "^26.2.0",
    "ts-node-dev": "^1.0.0-pre.57",
    "typescript": "^3.9.7"
  },
  "_moduleAliases": {
    "@": "dist"
  }
}
